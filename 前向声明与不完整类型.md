不完整类型精要

定义
声明但未完整定义的类型，编译器仅知其存在而不知其细节（大小、成员等）。

常见情形

前向声明类：class MyClass;

未知大小数组：extern int[];

void 类型

核心限制（不可进行以下操作）

定义对象 MyClass obj; ❌

获取大小 sizeof(MyClass) ❌

实例化对象 new MyClass ❌

访问成员 obj.member ❌

允许操作

定义指针/引用 MyClass* ptr; ✅

作为函数参数 void f(MyClass p); ✅

典型应用

循环依赖 - 相互引用时前向声明

PIMPL模式 - 隐藏实现细节

工厂模式 - 返回基类指针

重要规则

模板参数必须完整

继承需要完整类型

异常对象需完整类型

完整化时机
通过类定义或类型别名变为完整类型。

理解不完整类型对管理头文件依赖和优化编译时间至关重要。
