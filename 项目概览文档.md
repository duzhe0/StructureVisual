# StructureVisual 项目概览

## 项目是什么

这是一个**数据结构与算法可视化学习工具**，就像给算法配了个"慢动作回放器"。它用图形动画的方式，把抽象的算法执行过程变成看得见、摸得着的画面，帮助学习者理解算法是怎么一步步工作的。

## 能做什么

这个工具主要做两件事：

### 1. 图算法可视化
把图（就是那些点和线的结构）画出来，然后演示各种算法怎么在图上游走：
- **深度优先搜索（DFS）**：像走迷宫一样，一条路走到黑，走不通再回头
- **广度优先搜索（BFS）**：像水波纹扩散，一层一层往外探索
- **最短路径算法（Dijkstra）**：找两点之间最短的路
- **最小生成树算法（Prim、Kruskal）**：用最少的边把所有点连起来
- **拓扑排序**：给有先后顺序的任务排个队

### 2. 排序算法可视化
把一堆数字画成柱状图，然后演示各种排序算法怎么把它们排整齐：
- 冒泡排序、选择排序、插入排序
- 快速排序、归并排序、堆排序
- 基数排序

## 怎么工作的

整个系统就像搭积木，分三层：

**第一层：数据层** - 存储和管理数据
- 图的顶点和边存在哪里
- 排序的数字存在哪里
- 算法执行到哪一步了

**第二层：控制层** - 处理用户操作
- 用户点"开始"按钮，控制器就告诉模型开始执行算法
- 用户调速度滑块，控制器就调整动画快慢
- 用户添加顶点，控制器就告诉模型加一个新点

**第三层：显示层** - 把数据画出来
- 把顶点画成圆圈，边画成线
- 把数字画成柱状图
- 算法执行时，用不同颜色标记当前处理的位置

这三层通过"信号"和"槽"（Qt的通信机制）连接，就像电话线一样，一层有变化就通知其他层更新。

## 主要组成部分

### 主窗口（MainWindow）
整个程序的"大房子"，里面分成左右两半：
- **左边**：控制面板，有各种按钮和设置
- **右边**：显示区域，算法执行的地方

### 图模型（GraphModel）
图的"管家"，负责：
- 记住图里有哪些顶点和边
- 执行图算法，生成每一步的"剧本"
- 把算法步骤告诉显示层，让它画出来

### 排序模型（SortModel）
排序的"管家"，负责：
- 记住要排序的数字
- 执行排序算法，记录每一步操作
- 告诉显示层哪个数字要移动、哪个要比较

### 控制器（AlgorithmController）
用户的"翻译官"，负责：
- 把用户的点击、输入转换成模型能理解的指令
- 协调模型和显示层，让它们配合工作
- 管理算法的开始、暂停、停止

### 可视化元素（VisualItem）
画图的"画笔"，包括：
- **顶点项（VertexItem）**：画圆圈表示图的顶点
- **边项（EdgeItem）**：画线表示图的边
- **柱状图项（BarItem）**：画柱子表示数字

## 使用场景

这个工具适合：
- **学生学算法**：看动画比看书更容易理解
- **老师教课**：可以动态演示，不用在黑板上画半天
- **面试准备**：可视化帮助记忆算法流程
- **算法研究**：观察算法在不同数据上的表现

## 技术特点

- **Qt框架**：用C++写的，界面流畅，跨平台
- **MVC架构**：数据、控制、显示分开，代码清晰好维护
- **动画系统**：用Qt的动画框架，效果流畅自然
- **可扩展设计**：想加新算法，按模板填代码就行

## 总结

这个项目本质上是一个"算法动画播放器"，把抽象的算法过程可视化，让学习变得直观有趣。它采用经典的三层架构，代码结构清晰，既适合学习使用，也适合作为Qt开发的参考项目。

