“我现在要重构这个 C++ 可视化项目，请帮我完成去模板化的容器实现。

    先定义一个具体的结构体 struct Element { int key; }。

    请为我实现以下非模板类：MyVector（动态数组）、MyQueue（循环数组实现）、MyStack（简单封装）。

    请为我实现一个基于 BST（二叉搜索树） 的 MyMap 类，用于替代 std::map。

    注意：严禁使用 <template>，严禁使用任何 std 容器，所有内存申请必须通过 new 和 delete 手动管理，并提供析构函数。”